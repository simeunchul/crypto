# 작업 보고서 규칙

모든 작업 완료 후 다음 형식으로 HTML 보고서를 작성한다:

- 위치: `docs/YYYY-MM-DD/<주제>.html` (오늘 날짜 기준)
  - 예: `docs/2026-04-25/B4실패와_해결과정.html`
- 디렉토리가 없으면 생성
- 보고서에 포함할 내용:
  - 무엇을 시도했는지 (시도/접근법)
  - 무엇이 실패했고 왜 실패했는지
  - 어떻게 해결했는지 / 최종 결과
  - 관련 파일·커밋 링크

## 작성 스타일 (필수)

보고서는 **퀀트/파생/머신러닝 비전공자도 끝까지 읽고 이해할 수 있도록** 작성한다.

### 1. 비전공자 친화 서술
- 모든 전문 용어는 처음 등장 시 한 줄로 풀어 설명한다
  - 예: `CVaR (Conditional Value-at-Risk, "최악의 5% 손실 평균")`
- 수식은 가능한 한 줄글로 풀어 쓰고, 수식이 꼭 필요할 때는 바로 아래에 "이 식이 말하는 것" 한 줄을 붙인다
- 결과 수치는 항상 "기준선 대비 몇 % 좋아졌는지" 같은 직관적 비교로 변환해서 함께 적는다

### 2. 시각화 자료 첨부 (필수)
다음 중 **최소 2종 이상** 포함한다:
- **표 (table)** — 비교/지표/시도 이력
- **차트** — 인라인 SVG 로 학습 곡선·막대·비교 그래프 (외부 이미지 의존 금지, 보고서 단일 파일로 자급자족)
- **도표 (diagram)** — 흐름도·아키텍처를 SVG 또는 ASCII 박스로 표현
- **callout box** — 핵심 결과·경고·인용을 강조 박스로

차트에는 항상 축 단위, 기준선(baseline), 목표선(target)을 명시하여 수치만 봐도 의미가 전달되도록 한다.

### 3. 용어 정리집 (Glossary) — 필수 섹션
보고서 끝에 `## 용어 정리집` 섹션을 두고, 본문에 등장한 모든 전문 용어를 다음 형식으로 정리한다:

```
| 용어 | 한 줄 설명 | 본문에서의 역할 |
```

- 최소 5개 이상, 본문에 등장하는 모든 약어/수식 기호/도메인 용어 포함
- "왜 이게 이 보고서에서 중요한가"를 한 줄로 덧붙여 사전 정의가 아닌 맥락 정의가 되게 함

### 4. 추천 보고서 구조
1. 한 줄 결과 (callout)
2. 무엇을 시도했는지 (배경 + 접근)
3. 무엇이 실패했고 왜 — 표 + 도표
4. 어떻게 해결했는지 — 코드/차트
5. 최종 결과 — 차트 + 비교표
6. 비전공자용 요약 (TL;DR)
7. 관련 파일/커밋
8. **용어 정리집**

## 예외
- 단순 질문 답변, 짧은 코드 수정 등은 보고서 생략 가능
- 사용자가 "보고서 생략"이라고 하면 작성하지 않음
- **커밋 분리·푸시·git 정리 같은 메타 작업**은 보고서 작성 불필요 (실제 작업 결과물이 아닌 기록 행위)
- **여러 docs 일괄 갱신 (resume/status/blog 동기화)** 도 보고서 작성 불필요 — 이미 다른 보고서가 narrative 주체이고, 이건 그 결과를 전파하는 행위
- 위 예외는 커밋 메시지에 변경 의도가 충분히 담겨 있어야 함

# 글쓰기 표기 규칙 (자소서/이력서 등 사용자 향 산출물)

- **단어 연결 구분자**: 본문에서 단어를 나열할 때 **'·' (가운뎃점) 대신 ', ' (쉼표 + 공백)** 을 사용한다.
  - 예: `확률론·통계학` → `확률론, 통계학`
  - 예: `가격책정·헤지·매크로` → `가격책정, 헤지, 매크로`
- 적용 범위: 자소서/이력서/cover letter/포트폴리오 본문 등 사용자가 직접 제출하는 산출물
- 예외: 제목·태그·코드/파일명·기존 문서의 일관성을 위한 경우에는 그대로 둘 수 있음

- **마크다운 강조 금지 (`**...**`, `*...*`)**: 자소서/이력서 등 사용자 향 산출물 본문에 마크다운 강조 표시를 넣지 않는다.
  - 이유: 자소서 폼에 붙여넣으면 `**`이 그대로 출력되거나 어색한 굵은 글씨로 처리되어 "AI가 작성한 글"처럼 보인다.
  - 예: `**고객에게는 자연어로**` → `고객에게는 자연어로`
  - 적용 범위: 자소서/이력서/cover letter/포트폴리오 본문
  - 예외: 채팅 답변에서 핵심 변경 처를 사용자에게 시각적으로 짚어줄 때(메타 설명)는 사용 가능. 단, 사용자가 그대로 복사해 갈 "본문 영역"에는 절대 넣지 않는다.
